// 콜백을 이용하여 다음과 같이 출력되도록 display B 함수를 완성하세요.

// 1. 오답을 입력함 => callback ; 이라고만 해서

function displayA() {
  console.log('A');
}

function displayB(callback) {
  setTimeout(() => {
    console.log('B');
    callback; // ==> callback(); 를 하지 않으면 callback 만 부른거라서 displayB(displayC); 로 이미 자동으로 displayB 다음에 C가 행해져야하는데
    // ==> callback ; 이라고만 해서 온전히 callback이 이뤄지지 않음. 그래서 B만 출력하고 끝난다?
  }, 2000);
}

function displayC() {
  console.log('C');
}

displayA();
displayB(displayC);
