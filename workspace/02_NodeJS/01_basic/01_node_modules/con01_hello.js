// 실행하기
// (1) 터미널 열기 : ctrl + shift + ` (백틱)
// (2) cmd로 변경 후 파일이 있는 곳으로 이동 ( 명령어 : cd, + Tab키를 눌러서 폴더 선택 가능) ==> 개인페이지 필기 확인
// (3) js파일이 있는 폴더 까지 왔다면, ' node 파일명.js ' 를 써서 엔터를 누르면 js 파일을 읽은 터미널 환경이 완성된다.
// (주의!!) 꼭 저장하고 실행하세요!

function hello(name) {
  // 함수 선언

  // 함수 정의
  //console.log(name + '님 안녕하세요.'); // 그동안은 이걸 실행할려면 HTML 파일이었기 때문에 라이브서버에서 가능했지만, 지금은 js파일이라 터미널로 확인해야한다.
  console.log(`${name}님, 안녕하세요?`);
}

hello('누오'); // 함수 호출
